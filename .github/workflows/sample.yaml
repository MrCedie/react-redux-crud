name: Conditional Test with Delay

on:
  push:

jobs:
  conditional-test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: true
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Delay for 10 seconds
        run: sleep 50s


